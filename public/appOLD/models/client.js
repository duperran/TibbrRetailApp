define([
   'jquery',
  'backbone'
], function($, Backbone) {
  var Client = Backbone.Model.extend({
                "id":null,
                "name":null, 
                
            });
  return Client;

});
